[Unit]
Description=etcd
Documentation=https://github.com/coreos

[Service]
Type=notify
ExecStart=/usr/local/bin/etcd \
  --name {{ ansible_hostname }} \
  --cert-file=/etc/etcd/kube-api.crt \
  --key-file=/etc/etcd/kube-api.key \
  --peer-cert-file=/etc/etcd/kube-api.crt \
  --peer-key-file=/etc/etcd/kube-api.key \
  --trusted-ca-file=/etc/etcd/ca.crt \
  --peer-trusted-ca-file=/etc/etcd/ca.crt \
  --peer-client-cert-auth \
  --client-cert-auth \
  --initial-advertise-peer-urls https://{{ ansible_all_ipv4_addresses | ipaddr('private') | first }}:2380 \
  --listen-peer-urls https://{{ ansible_all_ipv4_addresses | ipaddr('private') | first }}:2380 \
  --listen-client-urls https://{{ ansible_all_ipv4_addresses | ipaddr('private') | first }}:2379,https://127.0.0.1:2379 \
  --advertise-client-urls https://{{ ansible_all_ipv4_addresses | ipaddr('private') | first }}:2379 \
  --initial-cluster-token etcd-cluster-0 \
  --initial-cluster {% for host in groups['etcd'] %}{{ host }}=https://{{ hostvars[host]['ansible_eth0']['ipv4']['address'] }}:2380{% if not loop.last %},{% endif %}{%- endfor %} \
  --initial-cluster-state new \
  --data-dir=/var/lib/etcd
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
